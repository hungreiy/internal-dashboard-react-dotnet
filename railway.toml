[build]
# No custom build command since Dockerfile handles build
dockerfilePath = "Dockerfile"
# Context is root directory, no need to set rootDir

[deploy]
# No custom start command, Dockerfile ENTRYPOINT will be used

[service]
name = "internal-dashboard-backend"